# 银行流水四层识别管线

识别流程固定为四层。每层只处理一种问题，并保留原始文件页码作为贯穿全流程的证据定位。

## 识别前：银行分拣确认

PDF 上传后不会立即提取流水。多模态模型先逐页分类为流水明细、账号列表、账户资料、调查令、银行回函、封面/目录、其他资料、空白页或待确认页，同时直接判断该页是“下一个银行材料区间开始”“继续上一银行区间”还是“不确定”，并提取可见的银行、调查令编号和账号证据。调查令首页、银行回函首页和银行抬头变化都只是判断银行切换的证据；同一银行内部的调查令、账户清单、多个账号和流水续页仍归在同一个连续区间。只有模型没有给出可靠的银行切换边界时，才使用相邻页面的银行证据作为兜底建议。确定性规则只负责保证页码连续、无重叠、无漏页，不替代模型作语义分档。

用户在同一个可横向拖动、可缩放的剪辑时间线上确认分段：原文件第 1 页到最后一页各出现一次，时间线上方的彩色括号标出每个银行材料区间的起止页，下面逐页显示缩略帧。点击任意帧时，时间线下方同步高清渲染原始 PDF 的对应单页；放大区没有独立翻页入口，页面切换始终以时间线为唯一控制源。缩略图只用于快速定位，不承担原文核对。用户可以直接修改括号上的银行名称，拖动括号端点调整分界、从当前页切开新银行区间、合并相邻区间、修改页面类型，并逐页决定是否进入流水识别。系统默认建议选择流水明细、账号列表、账户资料、银行回函和无法判断的页面，默认排除调查令、封面、空白页及其他行政材料；用户可随时覆盖建议或一键恢复。

只有当 1 到末页全部归类、没有重复页、每段页码连续、银行名称已确认、不存在“待确认”页面类型且至少选择一页时，系统才生成独立 PDF 并逐个进入下述识别管线。识别文件只复制用户选中的原始 PDF 页面；缩略图仅用于界面预览，不作为识别原件。

## 1. 页面识别

输入原始 PDF，输出每页的候选页面类型、银行、账号、页面方向、密度和置信度。这里的银行与账号只是页面证据，不能直接决定最终账户实体。无法确认的页面保留为 `UNKNOWN`。

## 2. 逻辑归档

根据连续页面上的银行和本方账号证据建立逻辑页段。空白背页和不确定续页跟随相邻页段；明确出现不同银行或本方账号时开始新页段。后续规范化仍可根据清晰页眉、账户清单和逐笔记录修正账户归属。

## 3. 流水提取

逻辑页段用于表达证据归属，物理识别批次用于控制模型输出量。系统根据页面类型和密度估算流水行数，动态生成批次，不使用固定页数切分。同一逻辑页段可以包含多个物理批次。

当服务明确返回输出容量错误时，只把失败批次按原页顺序二分。临时服务错误按退避策略有限重试，不通过拆页制造更多并发请求。

## 4. 完整性验证与补识别

每页独立检查页面覆盖、清点笔数、实际返回笔数和交易页空结果。缺页、漏行和交易页空结果触发单页补识别；字段置信度较低但笔数完整时生成字段核对任务，不重复整页识别。

补识别后仍不完整的页面写入阻断性 `PDF 解析不完整` 记录，禁止进入最终资金分析。成功页面立即缓存，后续重试只处理未通过的页面。

## 识别之后

账户别名归并、重复流水归并、平账、内部转账、资金流向和风险规则读取规范数据，不参与上述四层识别。任何修正都必须保留原始观察、证据位置和修正依据。
