# 银行流水四层识别管线

识别流程固定为四层。每层只处理一种问题，并保留原始文件页码作为贯穿全流程的证据定位。

## 识别前：银行分拣确认

PDF 上传后不会立即提取流水。系统先根据页面证据提出“银行名称 - 原文件页码”分拣方案，用户可以修改银行名称、页码范围或新增、删除分组。只有当 1 到末页全部覆盖、没有重复页且银行名称已确认时，系统才生成各银行独立 PDF 并逐个进入下述识别管线。分文件复制原始 PDF 页面，不把原件转换成图片。

## 1. 页面识别

输入原始 PDF，输出每页的候选页面类型、银行、账号、页面方向、密度和置信度。这里的银行与账号只是页面证据，不能直接决定最终账户实体。无法确认的页面保留为 `UNKNOWN`。

## 2. 逻辑归档

根据连续页面上的银行和本方账号证据建立逻辑页段。空白背页和不确定续页跟随相邻页段；明确出现不同银行或本方账号时开始新页段。后续规范化仍可根据清晰页眉、账户清单和逐笔记录修正账户归属。

## 3. 流水提取

逻辑页段用于表达证据归属，物理识别批次用于控制模型输出量。系统根据页面类型和密度估算流水行数，动态生成批次，不使用固定页数切分。同一逻辑页段可以包含多个物理批次。

当服务明确返回输出容量错误时，只把失败批次按原页顺序二分。临时服务错误按退避策略有限重试，不通过拆页制造更多并发请求。

## 4. 完整性验证与补识别

每页独立检查页面覆盖、清点笔数、实际返回笔数和交易页空结果。缺页、漏行和交易页空结果触发单页补识别；字段置信度较低但笔数完整时生成字段核对任务，不重复整页识别。

补识别后仍不完整的页面写入阻断性 `PDF 解析不完整` 记录，禁止进入最终资金分析。成功页面立即缓存，后续重试只处理未通过的页面。

## 识别之后

账户别名归并、重复流水归并、平账、内部转账、资金流向和风险规则读取规范数据，不参与上述四层识别。任何修正都必须保留原始观察、证据位置和修正依据。
